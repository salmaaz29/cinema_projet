{% extends 'base.html.twig' %}

{% block title %}Films index{% endblock %}

{% block body %}
       <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f4f7f6;
            color: #333;
            margin: 0;
            padding: 20px;
        }

        h1 {
            text-align: center;
            font-size: 2.5em;
            color: #2c3e50;
            margin-bottom: 30px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .table {
            width: 100%;
            border-collapse: collapse;
            margin: 0 auto;
            background-color: #ffffff;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            border-radius: 12px;
            overflow: hidden;
        }

        .table thead {
            background: linear-gradient(135deg, #2980b9, #3498db);
            color: #ffffff;
        }

        .table th,
        .table td {
            padding: 15px;
            text-align: left;
            border-bottom: 1px solid #e0e0e0;
        }

        .table th {
            font-weight: 600;
            text-transform: uppercase;
            font-size: 0.9em;
            letter-spacing: 1px;
        }

        .table tbody tr {
            transition: background-color 0.3s ease;
        }

        .table tbody tr:hover {
            background-color: #f8f9fa;
        }

        .table tbody tr:nth-child(even) {
            background-color: #fafafa;
        }

        .table tbody tr:nth-child(even):hover {
            background-color: #f8f9fa;
        }

        .table td a {
            text-decoration: none;
            color: #3498db;
            font-weight: 500;
            margin-right: 10px;
            transition: color 0.3s ease;
        }

        .table td a:hover {
            color: #2980b9;
        }

        .table td a:last-child {
            margin-right: 0;
        }

        .create-new-btn {
            display: inline-block;
            margin-top: 20px;
            padding: 12px 25px;
            background: linear-gradient(135deg, #2980b9, #3498db);
            color: #ffffff;
            text-decoration: none;
            border-radius: 8px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .create-new-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.25);
        }

        .no-records {
            text-align: center;
            color: #6c757d;
            font-style: italic;
            padding: 20px;
        }
    </style>

    <h1>Liste des films</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id_film</th>
                <th>Titre</th>
                <th>Description</th>
                <th>Date_sortie</th>
                <th>Duree</th>
                <th>Genre</th>
                <th>Realisateur</th>
                <th>Acteurs</th>
                <th>Affiche_url</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for film in films %}
            <tr>
                <td>{{ film.id }}</td>
                <td>{{ film.titre }}</td>
                <td>{{ film.description }}</td>
                <td>{{ film.dateSortie|date('d-m-Y') }}</td>
                <td>{{ film.duree }}</td>
                <td>{{ film.genre }}</td>
                <td>{{ film.realisateur }}</td>
                <td>{{ film.acteurs }}</td>
                <td>{{ film.afficheUrl }}</td>
                <td>
                    <a href="{{ path('app_admin_films_show', {'id': film.id}) }}">Voir</a>
                
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="10" class="no-records">Aucun enregistrement trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_admin_films_new') }}" class="create-new-btn">Créer un nouveau film</a>
{% endblock %}